{
  "name": "lee-website",
  "version": "1.0.0",
  "private": true,
  "description": "Lee Wilkers website",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build:public": "node scripts/clean-build.js && eleventy && npm test && node scripts/build-public.js",
    "build": "npm run build:public",
    "start": "node scripts/preview.js",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@11ty/eleventy": "3.1.5",
    "@11ty/eleventy-img": "6.0.4",
    "gray-matter": "4.0.3",
    "htmlparser2": "7.2.0"
  }
}
